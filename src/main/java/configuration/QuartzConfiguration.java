package configuration;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.ComponentScan;import org.springframework.context.annotation.Configuration;import org.springframework.scheduling.quartz.CronTriggerFactoryBean;import org.springframework.scheduling.quartz.JobDetailFactoryBean;import org.springframework.scheduling.quartz.SchedulerFactoryBean;import services.RegistruService;import services.jobs.DeleteUnusedRezervariJob;import java.util.HashMap;import java.util.Map;@Configuration@ComponentScan("services.jobs")public class QuartzConfiguration {    @Autowired    private RegistruService registruService;    @Bean    public JobDetailFactoryBean jobDetailFactoryBean() {        JobDetailFactoryBean factory = new JobDetailFactoryBean();        factory.setJobClass(DeleteUnusedRezervariJob.class);        Map<String, Object> map = new HashMap<>();        map.put(DeleteUnusedRezervariJob.REGISTRU_SERVICE, registruService);        factory.setJobDataAsMap(map);        factory.setName("deleteRezervari");        return factory;    }    //Job is scheduled after every day a 2AM    @Bean    public CronTriggerFactoryBean cronTriggerFactoryBean() {        CronTriggerFactoryBean stFactory = new CronTriggerFactoryBean();        stFactory.setJobDetail(jobDetailFactoryBean().getObject());        stFactory.setStartDelay(3000);        stFactory.setName("deleteRezervariTrigger");        stFactory.setCronExpression("0 0 2 ? * MON-FRI *");        return stFactory;    }    @Bean    public SchedulerFactoryBean schedulerFactoryBean() {        SchedulerFactoryBean scheduler = new SchedulerFactoryBean();        scheduler.setTriggers(cronTriggerFactoryBean().getObject());        return scheduler;    }}